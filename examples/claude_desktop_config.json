{
  "mcpServers": {
    "azure": {
      "command": "python",
      "args": ["-m", "azure_mcp.server", "start"],
      "env": {
        "AZURE_SUBSCRIPTION_ID": "your-subscription-id-here"
      }
    }
  }
}
